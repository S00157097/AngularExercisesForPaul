(function () {
    'use strict';

    /*angular.module('common.services')
        .service('orderResources', ['appSettings', '$resource',
            function (appSettings, $resource) {

                this.resource = () =>
                    $resource(appSettings.serverPath + '/api/orders')
            }
        ]);*/
});